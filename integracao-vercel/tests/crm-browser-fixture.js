// Isolated browser fixture: no production data or requests.
import {id} from './fixture.js';
export function instalarCRMFixture(base) {
 base.integracao_moradores.push({colecao:'processos',registro_id:id(4),referencia_tabela:'fin_receb_clientes',referencia_id:id(4),dados:{nucleoId:id(5)}});
 base.profiles.push({id:id(70),nome:'Ana Comercial',tipo:'Comercial',ativo:true},{id:id(71),nome:'Bia Comercial',tipo:'Comercial',ativo:true});
 const card={id:id(200),cliente_id:id(4),responsavel_id:id(70),status:'Cliente novo',nome:'Morador Teste',cpf_cnpj:'52998224725',telefone:'47999990000',municipio:'Município teste',municipio_id:id(2),remessa_id:id(3),remessa:'Remessa teste',origem:'manual'};
 base.integracao_crm_cards=[card,{id:id(201),lead_nome:'Lead de demonstração',lead_telefone:'47988880000',responsavel_id:id(70),status:'Cliente novo',origem:'chatwoot'}];
 base.integracao_crm_arquivo=[];
 base.integracao_crm_institucionais=[];base.integracao_crm_institucionais_followups=[];
 base.integracao_crm_followups=[];base.integracao_crm_chatwoot_resumo=[{card_id:id(200),conversas:2,mensagens:12,ultima_mensagem:new Date().toISOString()}];
 base.integracao_crm_tarefas=[];base.integracao_crm_atendimentos=[];base.integracao_crm_conversas=[];base.integracao_crm_mensagens=[];base.integracao_crm_agentes=[];
 base.integracao_semanal_municipios=[{id:id(210),municipio_id:id(2),nucleo_id:id(5),semana_padrao:1,ativo:true,telefone:'4733330000',observacoes:'Retorno semanal'}];
 for(const t of ['semanas','registros','arquivos','exclusoes'])base['integracao_semanal_'+t]=[];
 if(new URLSearchParams(location.search).has('semanal')){
  const cidades=[["Araranguá","SC",1],["Barracão","PR",1],["Braço do Trombudo","SC",1],["Chapadão do Lageado","SC",1],["Ibirama","SC",1],["Joinville","SC",1],["José Boiteux","SC",1],["Mafra","SC",1],["Major Vieira","SC",1],["Navegantes","SC",1],["Salete","SC",1],["Blumenau","SC",2],["Camboriú","SC",2],["Ilhota","SC",2],["Mirim Doce","SC",2],["Presidente Getúlio","SC",2],["Presidente Nereu","SC",2],["Rio do Campo","SC",2],["São José do Ouro","RS",2],["Vitor Meireles","SC",2],["Witmarsum","SC",2],["Águas Mornas","SC",3],["Capão Bonito do Sul","RS",3],["Caçapava","SP",3],["Doutor Pedrinho","SC",3],["Gaspar","SC",3],["Guaratinguetá","SP",3],["Guaratuba","PR",3],["São João Batista","SC",3],["Agrolândia","SC",3],["São Simão","SP",3],["Taió","SC",3],["Aurora","SC",4],["Imbuia","SC",4],["Lontras","SC",4],["Morro Agudo","SP",4],["Rio do Sul","SC",4],["Santa Terezinha","SC",4],["Trombudo Central","SC",4]];
  base.fin_receb_municipios.push(...cidades.map(([nome,uf],i)=>({id:id(300+i),nome,uf})));
  base.integracao_semanal_municipios=cidades.map(([, ,semana_padrao],i)=>({id:id(400+i),municipio_id:id(300+i),semana_padrao,ativo:true}));
  base.integracao_semanal_semanas=[0,1].map(i=>({id:id(500+i),municipio_id:id(400+i),ano:2026,mes:9,semana:1,concluido:true}));
  base.integracao_semanal_registros=[0,1].map(i=>({id:id(510+i),semana_id:id(500+i),comentario:'Registro fictício para conferir o layout.',created_at:'2026-09-01T14:30:00Z'}));
 }
 base.integracao_rotina_colunas=[];base.integracao_marketing_grupos=[];base.integracao_marketing_atualizacoes=[];base.integracao_marketing_rotina=[];
 base.integracao_marketing_etapas=[{id:id(220),fase_numero:1,fase_nome:'Boas-vindas',codigo:'M1',ordem:1,titulo:'Apresentar equipe',descricao:'Mensagem inicial no grupo de moradores.'},{id:id(221),fase_numero:2,fase_nome:'Acompanhamento',codigo:'M2',ordem:1,titulo:'Atualizar moradores'}];
 base.integracao_marketing_projetos=[{id:id(222),nucleo_id:id(5),ativo:true}];base.integracao_marketing_progresso=[];base.integracao_nucleo_ia=[];
 base.processos_kanban_andamentos=[{id:id(230),processo_id:id(5),status:'Topografia',status_operacional:'Em andamento',descricao_cliente:'Levantamento em conferência.',observacao_interna:'Nota interna de teste',data_atualizacao:'2026-09-19',visivel_ia:false}];
 return (url,options,json)=>{
  const table=url.pathname.split('/').at(-1),method=options.method||'GET';
  if(table==='fin_receb_municipios'||table==='fin_receb_remessas'){let rows=base[table];const municipio=url.searchParams.get('municipio_id');if(municipio)rows=rows.filter(r=>r.municipio_id===municipio.slice(3));return json(rows);}
  if(['integracao_crm_cadastrar_lead','integracao_crm_cadastrar_lead_compartilhado'].includes(table)){const p=JSON.parse(options.body);base.integracao_crm_cards.push({id:p.p_id,lead_nome:p.p_nome,lead_telefone:p.p_telefone,lead_municipio_id:p.p_municipio,municipio_id:p.p_municipio,municipio:base.fin_receb_municipios.find(m=>m.id===p.p_municipio).nome,status:'Cliente novo',responsavel_id:p.p_responsaveis?.[0]||p.p_responsavel||id(1),responsaveis_ids:p.p_responsaveis||[p.p_responsavel||id(1)],origem:'manual'});return json(p.p_id);}
  if(table==='integracao_crm_definir_comerciais'){const p=JSON.parse(options.body),c=base.integracao_crm_cards.find(c=>c.id===p.p_card);c.responsaveis_ids=p.p_responsaveis;c.responsavel_id=p.p_responsaveis[0];return json(null);}
  if(table==='integracao_crm_converter_lead'){const p=JSON.parse(options.body),c=base.integracao_crm_cards.find(c=>c.id===p.p_card),r=base.fin_receb_remessas.find(r=>r.id===p.p_remessa);Object.assign(c,p.p_dados,{cliente_id:id(250),remessa_id:r.id,remessa:r.nome});base.fin_receb_clientes.push({id:id(250),nome:c.lead_nome,municipio_id:p.p_municipio,remessa_id:r.id,ativo:true});return json(id(250));}
  if(['integracao_crm_followup','integracao_crm_institucional_followup'].includes(table)){const lista=table==='integracao_crm_followup'?base.integracao_crm_followups:base.integracao_crm_institucionais_followups;const p=JSON.parse(options.body),agora=new Date().toISOString();const anterior=lista.find(f=>f.card_id===p.p_card&&f.status==='pendente');if(anterior)Object.assign(anterior,{status:'feito',resumo:p.p_resumo,concluido_por:id(1),concluido_em:agora});const novo={id:crypto.randomUUID(),card_id:p.p_card,pedido_id:p.p_pedido,criado_por:id(1),criado_em:agora,previsto_em:new Date(Date.now()+p.p_dias*86400000).toISOString(),prazo_dias:p.p_dias,status:'pendente'};lista.push(novo);return json(novo.id);}
  if(table==='integracao_crm_salvar_institucional'){const p=JSON.parse(options.body),agora=new Date().toISOString(),c={id:p.p_id,nome:p.p_nome,contato:p.p_contato,produto:p.p_produto,valor:p.p_valor,responsavel_id:p.p_responsavel,status:p.p_status,motivo_perda:p.p_motivo,versao:p.p_versao+1,atualizado_em:agora};if(!p.p_versao){base.integracao_crm_institucionais.push(c);base.integracao_crm_institucionais_followups.push({id:crypto.randomUUID(),card_id:c.id,status:'pendente',prazo_dias:p.p_dias,criado_em:agora,previsto_em:new Date(Date.now()+p.p_dias*86400000).toISOString()});}else{Object.assign(base.integracao_crm_institucionais.find(r=>r.id===c.id),c);if(c.status!=='Em negociação')base.integracao_crm_institucionais_followups.filter(f=>f.card_id===c.id&&f.status==='pendente').forEach(f=>f.status='cancelado');}return json(c.id);}
  if(table==='integracao_crm_chatwoot_resumo'&&url.searchParams.get('order')!=='card_id')return json({message:'column integracao_crm_chatwoot_resumo.id does not exist'},400);
  if(table==='integracao_crm_arquivar_lead'){const p=JSON.parse(options.body),c=base.integracao_crm_cards.find(c=>c.id===p.p_card),agora=new Date().toISOString();if(p.p_arquivar){base.integracao_crm_arquivo.push({id:crypto.randomUUID(),card_id:c.id,dados:{...structuredClone(c),followups:base.integracao_crm_followups.filter(f=>f.card_id===c.id)},motivo:p.p_motivo,arquivado_em:agora,arquivado_por:id(1)});c.arquivado_em=agora;base.integracao_crm_followups.filter(f=>f.card_id===c.id&&f.status==='pendente').forEach(f=>f.status='arquivado');}else{c.arquivado_em=null;c.status='Cliente novo';c.responsavel_id=p.p_responsavel;Object.assign(base.integracao_crm_arquivo.find(r=>r.card_id===c.id&&!r.restaurado_em),{restaurado_em:agora,restaurado_por:id(1)});}return json(null);}
  if(table==='integracao_crm_dashboard')return json({agentes:base.profiles.filter(p=>p.tipo==='Comercial').map(p=>({id:p.id,nome:p.nome,ativo:p.ativo,carteira:2,movimentacoes:3,conversoes:1,perdas:0,sem_followup:1,pendentes:1,atrasados:0,followups_feitos:base.integracao_crm_followups.filter(f=>f.status==='feito').length,no_prazo:1,tempo_medio_horas:12,atraso_medio_horas:0,atendimentos_chatwoot:2,mensagens_enviadas:8,conversas_carteira:2,atendimentos_manuais:0})),cobertura:{conversas:2,mensagens:12,saidas_sem_autoria:0,humanas_sem_vinculo:0,automaticas:2,ultima_mensagem:new Date().toISOString()},movimentacoes:[]});
  if(table==='integracao_crm_editar_card'){const p=JSON.parse(options.body),c=base.integracao_crm_cards.find(c=>c.id===p.p_card),m=base.fin_receb_municipios.find(m=>m.id===p.p_municipio);if(c.cliente_id)Object.assign(c,{nome:p.p_nome,telefone:p.p_telefone,cpf_cnpj:p.p_cpf||null,nucleo_id:p.p_nucleo});else Object.assign(c,{lead_nome:p.p_nome,lead_telefone:p.p_telefone||null,cpf_cnpj:p.p_cpf||null,lead_municipio_id:p.p_municipio,municipio_id:p.p_municipio,municipio:m?.nome,nucleo_id:p.p_nucleo});return json(null);}
  if(table==='integracao_crm_relatorio_ativacoes'){const p=JSON.parse(options.body);return json(base.integracao_crm_cards.filter(c=>c.status==='Cliente ativo').map(c=>({card_id:c.id,cliente_id:c.cliente_id,nome:c.nome||c.lead_nome,cidade:c.municipio,valor:c.valor_total??null,responsavel_id:c.responsavel_id,comerciais_adicionais:[],ativado_em:c.ativado_em||new Date().toISOString(),origem:'registro'})).filter(r=>r.ativado_em.slice(0,10)>=p.p_inicio&&r.ativado_em.slice(0,10)<=p.p_fim));}
  if(table==='integracao_crm_transferir'){const p=JSON.parse(options.body);base.integracao_crm_cards.find(c=>c.id===p.card).responsavel_id=p.destino;return json(null);}
  if(table==='integracao_semanal_decidir'){const p=JSON.parse(options.body),r=base.integracao_semanal_exclusoes.find(r=>r.id===p.pedido);r.status=p.aprovar?'aprovado':'recusado';if(p.aprovar)base.integracao_semanal_municipios.find(c=>c.id===r.municipio_id).ativo=false;return json(null);}
  if(!/^(integracao_crm_|integracao_semanal_|integracao_marketing_|integracao_rotina_|integracao_nucleo_ia|processos_kanban_andamentos)/.test(table))return undefined;
  let rows=table==='integracao_crm_funil'?base.integracao_crm_cards.filter(c=>!c.arquivado_em):base[table];if(!rows)return json({message:'Tabela não simulada'},400);
  const matches=r=>[...url.searchParams].every(([k,v])=>v.startsWith('eq.')?String(r[k])===v.slice(3):v.startsWith('in.(')?v.slice(4,-1).split(',').includes(String(r[k])):true);
  if(method==='POST'){const r={id:crypto.randomUUID(),created_at:new Date().toISOString(),concluida:false,concluido:false,ativo:true,responsavel_id:id(1),...(table==='integracao_marketing_rotina'?{modulo:'marketing'}:{}),...JSON.parse(options.body)};rows.push(r);return json([r]);}
  if(method==='DELETE'){const found=rows.filter(matches);base[table]=rows.filter(r=>!found.includes(r));return json(found);}
  if(method==='PATCH'){const found=rows.filter(matches);found.forEach(r=>Object.assign(r,JSON.parse(options.body)));return json(found);}
  const offset=Number(url.searchParams.get('offset')||0);return json(rows.filter(matches).slice(offset,offset+500));
 };
}
