const {chromium}=require(process.env.PLAYWRIGHT_MODULE||'playwright');const assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'chrome',headless:true});try{const p=await b.newPage();p.setDefaultTimeout(12000);await p.goto((process.env.TEST_URL||'http://127.0.0.1:5187')+'/tests/browser.html?oficios');await p.getByLabel('E-mail',{exact:true}).fill('teste@example.invalid');await p.getByLabel('Senha',{exact:true}).fill('fixture');await p.getByRole('button',{name:'Entrar',exact:true}).click();await p.getByRole('navigation').getByRole('button',{name:'Configurações',exact:true}).click();await p.getByRole('tab',{name:'Modelos e representantes',exact:true}).click();await p.getByLabel('Introdução padrão do ofício',{exact:true}).fill('Introdução salva nas configurações.');await p.getByRole('button',{name:'Salvar modelo de ofício',exact:true}).click();await p.waitForFunction(()=>window.baseFixture.integracao_configuracoes.some(x=>x.registro_id==='modelosDoc'&&x.dados.valor.oficio?.introducao==='Introdução salva nas configurações.'));await p.getByRole('navigation').getByRole('button',{name:'Metas',exact:true}).click();await p.getByRole('button',{name:'Ofícios',exact:true}).click();await p.getByRole('button',{name:'Gerar ofício',exact:true}).click();assert.equal(await p.getByLabel('Introdução do ofício',{exact:true}).inputValue(),'Introdução salva nas configurações.');console.log('PASS modelo salvo nas configurações e reutilizado no gerador');}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1});
